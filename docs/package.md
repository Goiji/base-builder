# Package

The repo is published and consumed from source. Contracts are written to
be dependency-light, importing only OpenZeppelin and forge-std.

To consume a single primitive, vendor the file into your own repo and
re-audit it in your context.
