# Changelog

All notable changes to this project are documented here.

The format is based on [Keep a Changelog](https://keepachangelog.com/).

## [Unreleased]

### Added

- Token templates: `BaseERC20`, `BaseERC721`, `BaseERC1155`.
- Distribution primitives: `TokenVesting`, `MerkleAirdrop`, `PaymentSplitter`.
- CREATE2 `BaseTokenFactory` plus on-chain `ContractRegistry`.
- Governance `Timelock` and `RewardVault`.
- Financial primitives: `BondingCurve` linear market and `Pair` stub.
- Foundry test suite and Base / Base Sepolia deploy scripts.
