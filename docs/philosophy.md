# Building in the open

Every contract here is developed incrementally and in public: a primitive
starts small, gathers tests and documentation, and is refactored openly.

This keeps the history legible, makes regressions reviewable, and signals
that the toolkit is genuinely maintained rather than a one-shot dump.

The public commit history on `main` is the source of truth for what has
actually shipped.
